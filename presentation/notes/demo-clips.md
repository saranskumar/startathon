# Demo — 5 minutes (live, not clips)

Event split: 5 min slides + 5 min demo. Q&A starts at 10:00. Prefer live over pre-recorded clips.

**One workflow.** Do not tour features.

## Script

1. Phone calibration (or a preset matching the brother: large targets, one reachable region, partial speech). Name the skip if you use a preset.
2. One complete task: discrete choice → confirm, then text (touch selects, simulated voice fills). Show confirm + interrupt.
3. Optional, if time: desktop inspector on one `code/mock` page. Ranked actions, one real Playwright click. Phone is **not** driving this yet.

## Say once, at the start

| Status | What |
|---|---|
| Simulated | Speech recognition (`SimulatedSpeechSource`) |
| Not built | TTS / narration; phone↔desktop transport |
| Self-built | Aperture Daily mocks — not WhatsApp, not a live third-party app |
| Functional | Calibration, profile, 4×4 input matrix, fusion UI, desktop ranking + act |

## Parked clips (not needed if the live demo holds)

Old 5–10s clip list is retired for this format. If the live phone demo is unsafe, record only:

- Calibration landing on a profile (~8s)
- Same task, two methods (~8s)
