# Slide outline

Concise on purpose — each row is what's said, not a script. Full nuance lives in the linked `docs/` source ([sources.md](sources.md)). "Clip #" refers to [demo-clips.md](demo-clips.md).

**Hard constraint: 15 minutes total for presentation + demo + Q&A combined** (per [docs/idea/21 — Meeting Notes: Presentation Prep](../../docs/idea/21-meeting-notes-presentation-prep.md)). Running order is **demo-first**, not demo-last: prove the claim, then explain it, per that meeting.

| # | Slide | On-slide | Said (≤2 sentences) | Clip |
|---|---|---|---|---|
| 1 | Live demo (opening) | — | Two profiles, one task, switched live. This is the actual proof, shown first — not a closing recap. | 4, 5 |
| 2 | Context | Photo/name, no logo, no revenue talk | "This isn't a revenue pitch — it's open source, built for public good. It exists because [Shreevardhan]'s brother has cerebral palsy and falls through every accessibility tool built for one fixed impairment." | — |
| 3 | Who's already served | 3-row table: no vision+good hands → screen reader; motor trouble+clear speech+sight → Voice Access; can type/speak clearly → AI agents | "Each existing tool works fine for one clean channel — watch. Our target user has none of these individually, but partial ability across more than one." | 1, 2 (live or clip) |
| 4 | The gap | One line: "no single tool, no cooperation between tools" + hardware analogy | "These people stitch together mismatched tools or depend on a caregiver. Adaptive input already exists as hardware — a foot trackball, a tongue-controlled mouse — we do the same thing in software, on the phone already in their hand." | — |
| 5 | Prior art — Dasher | Dasher zoom clip | "Free-text entry without speech or fine taps already has a real answer — Cambridge/GNOME's Dasher. We cite it, we don't rebuild it." | 3 |
| 6 | The model | Capability profile diagram (continuous axes, not labels) | "We measure real ability per axis — touch precision, speech clarity, vision — instead of assuming a disability label." | — |
| 7 | Input composition | Split screen: same task, two profiles | "Same task, two different people, two different rendered input surfaces — composed live from the measured profile." | 5 |
| 8 | Output composition | Bluetooth-toggle worked example | "Low-precision users don't get a full screen readout — they get the two buttons that matter, spoken, right now." | 6 (if ready) |
| 9 | Why this is defensible | One line + citation + AI-scope correction | "Continuous ability modeling and modality fusion both have academic precedent. And to be clear — this isn't an AI product; adaptive input is the core, AI only closes small inference gaps, under confirmation." | — |
| 10 | Close | Market line + open-source line | "India's assistive-tech market is ~$2.5B today; we're building this open source so the adaptive layer can be adopted and localized, not gated." | — |

## Editing rule

Change the story here first. Only touch `slides.html` after this table is settled — the HTML should never be the place a structural decision gets made for the first time.
