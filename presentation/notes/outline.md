# Slide outline

Concise on purpose — each row is what's said, not a script. Full nuance lives in the linked `docs/` source ([sources.md](sources.md)). "Clip #" refers to [demo-clips.md](demo-clips.md).

| # | Slide | On-slide | Said (≤2 sentences) | Clip |
|---|---|---|---|---|
| 1 | Origin | Photo/name, no logo, no revenue talk | "This isn't a revenue pitch — it's open source, built for public good. It exists because [Shreevardhan]'s brother has cerebral palsy and falls through every accessibility tool built for one fixed impairment." | — |
| 2 | Who's already served | 3-row table: no vision+good hands → screen reader; motor trouble+clear speech+sight → Voice Access; can type/speak clearly → AI agents | "Each existing tool assumes you have one clean channel left. Our target user has none of those individually — but partial ability across more than one." | 1, 2 |
| 3 | The gap | One line: "no single tool, no cooperation between tools" | "These people either stitch together mismatched tools, or depend on a caregiver — losing independence on tasks they should manage alone." | — |
| 4 | Prior art — Dasher | Dasher zoom clip | "Free-text entry without speech or fine taps already has a real answer — Cambridge/GNOME's Dasher. We cite it, we don't rebuild it." | 3 |
| 5 | The model | Capability profile diagram (continuous axes, not labels) | "We measure real ability per axis — touch precision, speech clarity, vision — instead of assuming a disability label." | — |
| 6 | Input composition | Split screen: same task, two profiles | "Same task, two different people, two different rendered input surfaces — composed live from the measured profile." | 5 |
| 7 | Output composition | Bluetooth-toggle worked example | "Low-precision users don't get a full screen readout — they get the two buttons that matter, spoken, right now." | 6 (if ready) |
| 8 | Why this is defensible | One line + citation | "Continuous ability modeling and modality fusion both have academic precedent — what's new is doing it live, on a phone, with an agent closing the inference gap in real time." | — |
| 9 | Live demo | — | Two profiles, one task, switched live. This is the actual proof, not a recording. | 4, 5 |
| 10 | Close | Market line + open-source line | "India's assistive-tech market is ~$2.5B today; we're building this open source so the adaptive layer can be adopted and localized, not gated." | — |

## Editing rule

Change the story here first. Only touch `slides.html` after this table is settled — the HTML should never be the place a structural decision gets made for the first time.
