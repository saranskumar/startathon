# Media assets

Actual clips referenced by [`../slides.html`](../slides.html). Naming follows the slide order:

```
01-origin.jpg          brother photo (slide 1; omitted if missing)
02-talkback.mp4        5–8s, slide 1 Gap
03-voice-access.mp4    5–8s, slide 1 Gap
```

See [`../notes/demo-clips.md`](../notes/demo-clips.md) for what each clip must show and its status. Every video clip is 5–10 seconds — trim before dropping it in here, don't rely on `slides.html` to crop it.

Nothing is committed here yet — the placeholders in `slides.html` render as labeled dashed boxes until a real file with the matching name is added and its `src`/`data-slot` wired in.
